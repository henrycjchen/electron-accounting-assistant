export interface IFormattedOutputData {
  code: string;
  buyCompany: string;
  date: number;
  product: string;
  productType: string;
  unit: string;
  count: number;
  notes: string;
}

export interface IFormattedInputData {
  date: number;
  product: string;
  unit: string;
  count: number;
}
