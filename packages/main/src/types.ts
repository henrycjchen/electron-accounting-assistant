export interface IFormattedOutputData {
  code: string;
  buyCompany: string;
  date: string;
  product: string;
  productType: string;
  unit: string;
  count: number;
  notes: string;
}

export interface IFormattedInputData {
  date: string;
  product: string;
  unit: string;
  count: number;
}
